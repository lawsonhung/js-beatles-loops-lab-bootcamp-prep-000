// add solution here
function theBeatlesPlay(musicians, instruments) {
  var array = [];
  
  for (let int i = 0; i < musicians.length; i++) {
    array.push(`${musicians[i]} plays ${instruments[i]}`);
  }
}